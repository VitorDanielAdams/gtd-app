import { ArrowUp, ArrowDown, Folder, Info } from "lucide-react-native";

export const icons: Record<string, React.ElementType> = {
  arrowUp: ArrowUp,
  arrowDown: ArrowDown,
  folder: Folder,
  info: Info,
};